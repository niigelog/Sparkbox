# Sparkbox 使用教程

在 X 上点 ❤️，帖子自动存进你自己的数据库，用侧边栏按文件夹管理。

---

## 目录

- [它是怎么工作的](#它是怎么工作的)
- [第一步：部署后端](#第一步部署后端)
- [第二步：安装插件](#第二步安装插件)
- [第三步：日常使用](#第三步日常使用)
- [改了代码之后](#改了代码之后)
- [出问题了怎么查](#出问题了怎么查)
- [安全须知](#安全须知)

---

## 它是怎么工作的

```
┌─────────────────────┐        ┌──────────────────┐       ┌────────────┐
│  x.com 页面          │  点❤️  │  插件后台         │ HTTP  │  API 服务   │
│  内容脚本抓取帖子     │──────▶│  Service Worker  │──────▶│  + Postgres │
└─────────────────────┘        └──────────────────┘       └─────┬──────┘
                                                                 │
┌─────────────────────┐                                          │
│  侧边栏（收藏库）     │◀─────────────────────────────────────────┘
│  文件夹管理、查看     │              直接读接口，不经过后台
└─────────────────────┘
```

三个要点：

- **没有本地缓存。** 点击直接写数据库，成败立刻反馈到页面上的提示条。后端没启动时这次点击会丢，但会明确告诉你。
- **身份自动识别。** 插件读你 X 的登录 cookie，数据自动归到你的账号下，不需要注册登录。
- **侧边栏不依赖插件后台。** 后台就算被浏览器回收，列表和文件夹管理照常可用。

---

## 第一步：部署后端

后端跑在你的服务器上（下面以 `192.168.101.12` 为例），复用已有的 Postgres 容器。

### 1.1 准备代码和配置

```bash
git clone <你的仓库地址> && cd Sparkbox
cp .env.example .env
```

编辑 `.env`：

```ini
# 数据库（宿主机上跑 npm test / db:migrate 时用这个地址）
APP_POSTGRES_HOST=192.168.101.12
APP_POSTGRES_PORT=5432
APP_POSTGRES_DB=sparkbox_db
APP_POSTGRES_USER=app_pg_user
APP_POSTGRES_PASSWORD=你的密码

# Docker：已有 Postgres 容器所在的网络和容器名
PG_NETWORK=local-db-stack_db_net
PG_CONTAINER=postgres-17

# API 在宿主机上的监听地址
API_BIND=0.0.0.0
```

> **`API_BIND` 必须改成 `0.0.0.0`。**
> 默认值 `127.0.0.1` 表示只有服务器自己能访问，你装在别的电脑上的插件是连不上的。
> 改成 `0.0.0.0` 之后同局域网的人都能访问 —— 先读完[安全须知](#安全须知)。

不确定网络名？查一下：

```bash
docker inspect -f '{{range $k,$v := .NetworkSettings.Networks}}{{$k}} {{end}}' postgres-17
```

### 1.2 启动

```bash
docker compose up -d
```

会依次做两件事：先跑数据库迁移，**成功退出后**才启动 API —— 所以 API 永远不会连上一个表结构没就绪的库。

`.env` 里必填项没配的话，compose 会直接拒绝启动并告诉你缺哪一个，不会起一个注定失败的容器：

```
required variable APP_POSTGRES_PASSWORD is missing a value: 请在 .env 里设置
```

> **改完 `.env` 要用 `--force-recreate` 重来。**
> `migrate` 是一次性容器（`restart: no`），失败退出后 `docker compose up -d` 不会重新跑它，
> `docker compose logs migrate` 拿到的还是**上一次**的旧日志 —— 看起来就像改了没生效。
>
> ```bash
> docker compose up -d --force-recreate
> ```

### 1.3 确认成功

```bash
docker compose logs migrate
```

应该看到迁移逐条应用完，容器退出：

```
✔ 0001_init.sql  57ms
✔ 0002_default_user.sql  12ms
...
迁移完成
```

再确认 API：

```bash
curl http://127.0.0.1:7000/health
# {"ok":true,"user":"00000000-0000-0000-0000-000000000001"}
```

`docker compose ps` 里 api 应该是 `healthy`。

---

## 第二步：安装插件

插件装在**你日常刷 X 的那台电脑**上。

### 2.1 配置后端地址

那台电脑上的 `.env`：

```ini
SYNC_TARGET=local
SYNC_ENDPOINT=http://192.168.101.12:7000/api/posts
```

### 2.2 打包

```bash
npm install
npm run build
```

构建完会打印出插件被授权访问哪些域名，确认里面有你的后端地址：

```
host_permissions: https://x.com/*  https://twitter.com/*  http://192.168.101.12:7000/*
```

> 这一行是**根据 `SYNC_ENDPOINT` 自动生成的**。改了后端地址只需重新 `npm run build`，权限会跟着变，不用手动改 manifest。

### 2.3 加载到 Chrome

1. 地址栏进 `chrome://extensions`
2. 右上角打开「开发者模式」
3. 点「加载已解压的扩展程序」
4. 选择项目里的 **`dist/`** 目录

### 2.4 验证能连通

在这台电脑上：

```bash
curl http://192.168.101.12:7000/health
```

**连不上**说明服务器上的 `API_BIND` 还是 `127.0.0.1`，回第一步改。

---

## 第三步：日常使用

### 收藏一条帖子

在 X 上**点 ❤️**（爱心）。左下角会弹出「已收藏到信息箱」。

书签按钮（🔖）也能触发收藏，效果一样，只是记录的来源不同。

### 打开收藏库

点浏览器工具栏上的插件图标，侧边栏会从右侧滑出。

```
┌────────────────────────────────┐
│ [信息箱 (3)      ▾]  ＋         │  ← 下拉选文件夹
├────────────────────────────────┤
│ 领哥LingGe🙏 @shangdu2005       │
│ 睡前灵魂拷问！                   │
│ 你精通Codex和Claude。            │
│ 查看更多                        │  ← 超过 6 行才出现
│ 原帖  [信息箱 ▾]  ❤️      🗑     │
├────────────────────────────────┤
│ Dalicat @onehajimi              │
│ ...                             │
└────────────────────────────────┘
```

| 操作 | 怎么做 |
|---|---|
| 切换文件夹 | 左上角下拉，括号里是条数 |
| 看全部收藏 | 下拉最后一项「全部」 |
| 新建文件夹 | 点 `＋`，建完自动切过去 |
| 重命名 / 删除文件夹 | 选中自建文件夹后，右边会出现按钮 |
| 移动帖子 | 卡片右下角的下拉 |
| 删除帖子 | 卡片右下角垃圾桶（鼠标移上去才显示） |
| 展开长正文 | 「查看更多」 |
| 打开原帖 | 「原帖」 |

### 取消收藏

在 X 上**取消 ❤️**，这条会从收藏库里撤掉。

> **只撤销「还没动过」的。** 如果你已经把它归到某个文件夹、或加过备注，取消点赞不会删掉它 —— 那是你的整理成果，不该被一次手滑抹掉。这种情况会提示「已归类到其他文件夹，保留」。

### 关于「信息箱」

信息箱就是**未分类**。新收藏默认都落在这里，不需要也不能删除它。

新用户首次使用时，系统会自动创建**文章 / 想法 / 观点 / 建议**四个文件夹。

---

## 改了代码之后

| 改了什么 | 怎么生效 |
|---|---|
| 后端代码 | 服务器上 `docker compose up -d --build` |
| 数据库结构 | 新建 `db/migrations/NNNN_描述.sql`，然后同上 |
| 插件代码 | `npm run build` → 扩展页点 ⟳ → **刷新 x.com 标签页** |

> **最后那步不能漏。** 扩展重新加载后，页面里已注入的内容脚本会失效，点收藏会弹「插件已更新，请刷新本页后再收藏」。刷新页面即可。

已经应用过的迁移文件**不能再改** —— 内容变了会被哈希校验拦下。要改结构就新建一个迁移文件。

---

## 出问题了怎么查

**先看提示条。** 失败时它会直接说原因，照着做通常就解决了。

| 现象 | 原因 | 解决 |
|---|---|---|
| 点 ❤️ 完全没反应 | 扩展被禁用或没装上 | `chrome://extensions` 看开关是否打开 |
| 「插件已更新，请刷新本页」 | 内容脚本失效 | 刷新 x.com 页面 |
| 「后端没有响应」 | API 没启动或连不上 | 服务器上 `docker compose ps` |
| 侧边栏顶部红条「连不上后端」 | 同上 | 同上 |
| 能收藏但别的电脑看不到 | `API_BIND` 是 `127.0.0.1` | 改成 `0.0.0.0` 后重新部署 |
| 改了 `.env` 但日志还是旧错误 | `migrate` 是一次性容器，不会自动重跑 | `docker compose up -d --force-recreate` |
| 「已归类到其他文件夹，保留」 | 正常行为 | 见[取消收藏](#取消收藏) |

### 看日志

服务器端：

```bash
docker compose logs -f api        # 所有写操作都会打出来
docker compose logs migrate       # 迁移是否成功
```

浏览器端：在 x.com 按 F12 打开 Console，搜 `sparkbox`。正常流程是：

```
[sparkbox] content script 已注入 https://x.com/home
[sparkbox] 点击命中 like ← tweet
[sparkbox] 识别为保存动作（来源 like）
[sparkbox] 已保存 1234567890
```

哪一步没出现，问题就在那一步：

- 没有「已注入」→ 扩展没运行，检查是否被禁用、是否刷新过页面
- 有「点击命中」但没有「识别为保存动作」→ X 改了按钮标识，需要更新选择器
- 到「识别为保存动作」就停 → 抓取失败，Console 里会有 `抓不到 tweetId`
- 最后一步失败 → 后端问题，看服务器日志

侧边栏也能单独调试：在侧边栏上右键 →「检查」。

---

## 安全须知

**接口没有任何鉴权。** 插件报上来的身份（你的 X 用户 ID）服务端无法验证真假 —— 这是识别，不是认证。

所以监听地址直接决定风险等级：

| `API_BIND` | 谁能访问 | 结论 |
|---|---|---|
| `127.0.0.1` | 只有服务器自己 | 安全 |
| `0.0.0.0` | **同局域网的任何人** | 全部收藏可被任意读写删 |

要让别的电脑上的插件能用，就必须是 `0.0.0.0`。这意味着：

- 同一个网络里的任何设备，只要知道 `192.168.101.12:7000`，就能读走你的全部收藏
- 也能删光它们
- 不需要任何密码

**在家庭/办公室内网自用可以接受，但绝不要把这个端口暴露到公网。** 需要多人使用或对外提供服务时，必须先接入 X OAuth，让服务端真正验证身份。

数据库端口默认不对外发布（走 Docker 内部网络），这一点不用额外操心。
